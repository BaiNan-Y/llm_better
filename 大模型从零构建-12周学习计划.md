# 大模型从零构建：12 周学习计划（架构演进路线）

> 适用对象：有软件开发经验 + 一定理论基础
> 核心目标：不依赖现成框架，**亲手从零复现 Transformer → GPT → Llama 的架构演进史**，训练一个 0.5B 规模的"自己的大模型"，彻底吃透大模型原理与训练工程。
> 核心方法论：**固定数据 + 固定参数量 + 固定算力，一次只改一处架构**，用 loss 曲线和生成质量量化每一处优化的价值。

---

## 目录

1. [计划概述与最终产出](#一计划概述与最终产出)
2. [技术路线总览：架构演进六阶段](#二技术路线总览架构演进六阶段)
3. [数据集方案详解](#三数据集方案详解)
4. [硬件与算力预算](#四硬件与算力预算)
5. [12 周详细计划](#五12-周详细计划)
6. [训练工程要点速查](#六训练工程要点速查)
7. [风险与对策](#七风险与对策)
8. [参考资源清单](#八参考资源清单)

---

## 一、计划概述与最终产出

### 学习哲学

```
你不需要"学会"大模型，你需要"拆开"它。

每一行代码你都亲手写过，
每一次架构改动你都亲眼看过 loss 曲线的变化，
这样的大模型，才是属于你的。
```

### 计划结束时你将拥有

| 产出 | 说明 |
|---|---|
| 一套自研代码库 | 从 tokenizer 到训练到推理，全部自己的实现（约 2000~4000 行） |
| 1 个训练好的 0.5B 中文/中英双语模型 | 可日常对话，可部署到 Ollama/vLLM |
| 1 份架构实验报告 | 6 个阶段的 loss 对比表 + 关键发现 |
| 完整训练工程能力 | 混合精度、梯度累积、数据清洗、分布式（可选） |
| 6 个里程碑 demo | 每个架构阶段都能独立演示的生成模型 |

### 时间投入假设

- 平均每天 2~3 小时，周末 5~6 小时（每周约 20 小时）
- 若时间更充裕，可压缩至 8 周；若更紧，可将 8~10 周合并

---

## 二、技术路线总览：架构演进六阶段

按模型发展史逐代实现，在每个阶段都训出可对比的模型。

### 阶段 0：Tokenizer 与数据管线（第 2 周）

**核心知识**：为什么需要 tokenizer？BPE（Byte Pair Encoding）算法原理；字节级 BPE；词汇表大小 vs 压缩率。

**参考实现**：Karpathy `minbpe`（minGPT 配套课程）。

**验证标准**：
- [ ] 自己实现的 tokenizer 在 TinyStories 上的压缩率接近 GPT-2 tokenizer
- [ ] encode/decode 可逆（round-trip 无损失）

### 阶段 1：原始 Transformer（2017，第 3~4 周）

**核心知识**：Scaled Dot-Product Attention；多头注意力；QKV 投影；Encoder-Decoder 结构；交叉注意力；Post-LN；绝对位置编码；残差连接。

**论文**：《Attention Is All You Need》——从头读到尾，公式逐个推导。

**验证标准**：
- [ ] 手写 attention 前向传播，输出形状正确
- [ ] 用 TinyStories 训练后能生成连贯短文本
- [ ] 记录 baseline loss（后续所有对比的锚点）

### 阶段 2：GPT-1 Decoder-only（2018，第 5 周）

**核心知识**：因果掩码（causal mask）；自回归生成；砍掉 Encoder 后为什么效果反而更好（训练目标统一、计算效率高）。

**关键实验**：同数据、同参数量下对比阶段 1 与阶段 2 的 loss。

**验证标准**：
- [ ] Decoder-only + causal mask 实现正确
- [ ] 对比表：阶段 2 的 loss 不劣于阶段 1（甚至更好），且训练更快

### 阶段 3：GPT-2 改进（2019，第 6~7 周）

**核心知识**：Pre-LN vs Post-LN（训练稳定性）；残差初始化缩放；层数加深、上下文加长；更大词汇表与更多参数。

**关键实验**：观察训练曲线稳定性对比；long-context 生成的连贯性。

**验证标准**：
- [ ] 训练 loss 曲线明显比 Post-LN 平滑、无发散
- [ ] 上下文 512→1024 后，长文本生成连贯性提升
- [ ] 实现 temperature / top-k / top-p 采样

### 阶段 4：Llama 化改造（2023，第 8 周）

**核心知识**：RMSNorm（省去均值，更稳更快）；RoPE 旋转位置编码（外推能力）；SwiGLU 门控 FFN；Grouped Query Attention（GQA，推理省显存）。

**关键实验**：逐项替换并在同一数据上对比，找出哪项改进对 loss 贡献最大。

**验证标准**：
- [ ] 四项改造全部实现并整合
- [ ] 对比表：阶段 4 相比阶段 3 的 loss 改善 + 显存占用下降
- [ ] 得到一个"Llama 风格"的 0.5B 模型

### 阶段 5：现代技巧（进阶可选，第 10 周穿插）

**核心知识**：Flash Attention（显存/速度优化）；MLA（DeepSeek 的注意力压缩）；Mamba 状态空间模型（注意力替代方案）。

**说明**：此阶段了解原理 + 选装集成即可，不作为主线。

---

## 三、数据集方案详解

### A. 架构实验期数据（跑通 + 逐阶段对比用）

| 数据集 | 语言 | 规模 | 用途 | 获取渠道 |
|---|---|---|---|---|
| **TinyStories** | 英文 | 单文件约 260MB | **首选**：专为小模型设计，半小时~数小时训出连贯模型 | HuggingFace / ModelScope |
| **WikiText-103** | 英文 | 1.8 亿 token | 经典基准，loss 可与论文对标 | HuggingFace |
| **TinyZh** | 中文 | 小 | 可选：中文热身 | ModelScope / GitHub |

> 实验期原则：**数据小到跑一次只要几小时**，才能支撑"一次只改一处"的高频对比实验。

### B. 正式训练期数据（中英混合）

| 数据集 | 语言 | 规模 | 说明 | 优先级 |
|---|---|---|---|---|
| **SkyPile-150B**（智源）| 中文 | 150GB | 质量高、已去重清洗，中文首选 | ⭐⭐⭐ |
| **MNBVC** | 中文 | 60TB | 覆盖新闻/小说/百科/论坛，按需取子集 | ⭐⭐ |
| **中文维基百科 dump** | 中文 | ~1.5GB | 高质量百科子集 | ⭐⭐⭐ |
| **SlimPajama** | 英文 | 627B token | 高质量通用英文 | ⭐⭐ |
| **FineWeb-Edu**（HF）| 英文 | 1.3T token | 2024 后质量标杆，抽子集 | ⭐⭐ |
| **悟道 WuDaoCorpus** | 中文 | 5TB | 经典，需自行清洗 | ⭐ |

> 注意：SkyPile、MNBVC 均托管于 HuggingFace（国内需镜像或 ModelScope 加速），建议提前验证下载连通性。

### 推荐混合配比（0.5B 小模型）

```
中文网页     50%
英文通用     25%
代码         10%   （GitHub 代码清洗后，选装）
百科/书籍    15%
```

### 数据量建议

- Chinchilla 法则：最优 token 数 ≈ 参数量 × 20 → 0.5B 约需 10~20B token
- **学习目的实际建议**：训练 **2~5B token**（约 5~15GB 文本），单卡 1~3 天可见明显效果
- 数据质量 > 数量：宁可 10GB 高质量，不要 100GB 噪音

### 数据清洗流水线清单（第 9 周实现）

- [ ] URL/HTML 标签剥离
- [ ] 敏感内容过滤（可选）
- [ ] 近似去重（MinHash）
- [ ] 行/段落长度过滤（过短丢弃，过长截断）
- [ ] 语言识别（过滤无关语言）
- [ ] 低质量内容启发式过滤（符号密度、重复度、困惑度）
- [ ] 格式规范化（统一空格、换行、引号）

---

## 四、硬件与算力预算

### 显存需求估算

| 模型规模 | 精度 | 权重 | 优化器状态 | 激活（估） | 总显存 | 推荐显卡 |
|---|---|---|---|---|---|---|
| 0.5B | BF16 | ~1GB | ~3GB | 3~8GB | 8~16GB | **RTX 4090 24GB** |
| 1.3B | BF16 | ~2.6GB | ~8GB | 6~15GB | 18~32GB | A100 40GB / 双 4090 |

### 训练时长粗算（0.5B，单卡 4090，BF16）

| 训练量 | 估算时长 | 用途 |
|---|---|---|
| 0.2~0.5B token | 2~6 小时 | 架构对比实验（阶段 1~4）|
| 2~5B token | 1~3 天 | 正式训练 |
| 10B token | 3~7 天 | 完整 Chinchilla 最优 |

### 成本预算（人民币）

| 阶段 | 资源 | 估算成本 |
|---|---|---|
| 第 1~8 周（架构实验）| 本地 RTX 3060/4060 或 4090 按量 | 0~1500 元 |
| 第 11 周（正式训练）| 4090 按小时 60~100h | 1200~2500 元 |
| 备用/迭代 | 4090/A100 按量 | 1000~2000 元 |
| **合计** | | **2500~6000 元** |

### 省钱策略

1. **架构实验全部在本地或最便宜实例跑小数据**，只有正式训练才上 4090/A100
2. 云 GPU 用**按量计费 + 磁盘快照**：环境配好打快照，训练完销毁，下次秒恢复
3. 训练前先用 1/100 数据做 smoke test（1~2 小时）验证管线正确，再开长任务
4. 国产平台（AutoDL/恒源云）通常比国际大厂便宜 40%+

---

## 五、12 周详细计划

> 约定：每周围绕 1 个主题；每日任务按 "D1~D7" 标注，可按实际节奏弹性调整（D 表示一次 2~3 小时的工作块）；每周末有验收节点。

---

### 第 1 周：环境搭建与 PyTorch 复健

**主题**：把工具链武装到牙齿，恢复 PyTorch 手感，建立项目骨架。

| 日期 | 任务 |
|---|---|
| D1 | 安装 Python 3.11+、PyTorch 2.x（CUDA 版）；验证 CUDA 可用：`torch.cuda.is_available()`；本地装 Ollama 并跑通 Qwen-1.5B 作为"参照物" |
| D2 | PyTorch 复健：tensor 操作、形状变换、自动求导 `autograd` |
| D3 | PyTorch 复健：`nn.Module`、`nn.Linear`/`nn.Embedding`、`Dataset`/`DataLoader`、`torch.optim`、train loop 模板 |
| D4 | 数学热身：矩阵乘法、softmax、交叉熵的手工 numpy 实现；推导 attention 的 QKV 公式 |
| D5 | 建立项目骨架：Git 仓库、目录结构（data/ / tok/ / models/ / train/ / utils/）、`requirements.txt`、README |
| D6 | 写一个 2 层 MLP + 训练循环，在随机数据上跑通完整流程（过拟合小样本验证）|
| D7 | **验收**：能在任意小数据集上训练一个 MLP 并可视化 loss 曲线；Ollama 中能正常与 Qwen 对话 |

**本周产出**：可复用的项目骨架 + PyTorch 环境验证报告。

---

### 第 2 周：BPE Tokenizer 与数据管线

**主题**：亲手实现 tokenizer，打通"原始文本 → 训练 batch"的全链路。

| 日期 | 任务 |
|---|---|
| D1 | 学习 BPE 算法原理（论文《Neural Machine Translation of Rare Words》）；阅读 `minbpe` 源码中的 base/basic 实现 |
| D2 | 实现 `BasicTokenizer`：训练 merges、encode/decode；用短文测试 round-trip 可逆 |
| D3 | 升级为 `RegexTokenizer`（GPT-2 风格：`\p{L}` 等正则预分词），加入特殊 token `<|endoftext|>` |
| D4 | 加入字节级回退（byte-level fallback），保证任意 Unicode 可编码；测试中文文本 |
| D5 | 下载 TinyStories 并构建数据管线：文本 → token id → 拼接为长数组 → 切分 train/valid → 保存 .bin |
| D6 | 实现 batch 采样器：随机窗口、batch 打包、`torch.load` 分片读取大文件 |
| D7 | **验收**：统计自研 tokenizer 的压缩率（token/字符）并对比 GPT-2；数据管线 1 分钟内产出 1000 个 batch |

**本周产出**：可运行的 tokenizer（含 BPE 训练）+ 高性能数据加载器。

---

### 第 3 周：注意力机制与 Transformer 数学

**主题**：不写一行模型代码，先把每个公式吃透（本周是全部后续工作的地基）。

| 日期 | 任务 |
|---|---|
| D1 | 用 numpy 手写 Scaled Dot-Product Attention，验证维度与 softmax 行为；测试注意力权重分布 |
| D2 | 理解 Multi-Head Attention：多头拼接逻辑、为什么除 `√d_k` |
| D3 | 推导并理解 QKV 投影矩阵的维度流：`d_model → d_k/h`；手画完整数据流图 |
| D4 | 理解 LayerNorm（对比 BatchNorm）、FFN、残差连接；手工计算一个 4 维示例 |
| D5 | 精读《Attention Is All You Need》第 3 节（Model Architecture），逐公式标注实现思路 |
| D6 | 理解 Post-LN vs Pre-LN 的区别与动机；理解绝对位置编码（sin/cos）公式 |
| D7 | **验收**：向自己（或文档）讲清楚"一张输入矩阵如何流过整个 Transformer"；numpy attention 与手算结果一致 |

**本周产出**：注意力/Transformer 数学笔记 + numpy 验证脚本。

---

### 第 4 周：实现原版 Transformer（阶段 1 完成）

**主题**：把纸上架构变成能训的代码，跑出第一个 baseline。

| 日期 | 任务 |
|---|---|
| D1 | 实现 `LayerNorm`、`MultiHeadAttention`（含 QKV、拼接、输出投影）|
| D2 | 实现 `PositionalEncoding`、`PositionwiseFeedForward`、`EncoderLayer` |
| D3 | 实现 `DecoderLayer`（含 masked self-attn + cross-attn）、堆叠 Encoder/Decoder |
| D4 | 实现训练循环：Adam + warmup + cosine 学习率；梯度裁剪；loss/checkpoint 记录 |
| D5 | 在 TinyStories 上小规模训练（如 0.2~0.5B token），观察 loss 是否持续下降 |
| D6 | 实现自回归生成（逐步 feed token）；测试输出连贯性；记录 baseline loss |
| D7 | **验收**：`torchsummary` 检查模型结构；生成 2~3 段连贯英文短故事；把 loss 曲线和生成样例存入实验记录 |

**本周产出**：完全手写的原版 Transformer + 训练代码 + 第一条 baseline。

> 里程碑：你已经有第一个"自己的 Transformer"了。

---

### 第 5 周：GPT-1 Decoder-only（阶段 2 完成）

**主题**：砍掉 Encoder，理解自回归范式，完成第一次架构对比实验。

| 日期 | 任务 |
|---|---|
| D1 | 理解因果掩码：上三角 mask 实现；与训练目标（预测下一个 token）的关系 |
| D2 | 实现 `CausalSelfAttention`（带 mask）+ 单个 `DecoderBlock` + `GPT` 类 |
| D3 | 实现完整 Decoder-only 模型 + 训练循环；确保与阶段 1 参数量大致对齐 |
| D4 | 在**同一 TinyStories 数据、同一参数量**下训练阶段 1 和阶段 2 |
| D5 | 分析对比：loss 曲线对比、训练吞吐对比、生成质量对比；记录实验表格 |
| D6 | 精读 GPT-1 论文《Improving Language Understanding by Generative Pre-Training》核心部分 |
| D7 | **验收**：完成"阶段1 vs 阶段2"对比实验报告；结论：Decoder-only 更简、更快、效果不输 |

**本周产出**：GPT-1 风格模型 + 第一次正式的架构对比实验。

---

### 第 6 周：GPT-2 改进（阶段 3 完成：Pre-LN + 残差缩放）

**主题**：体验"训练稳定性"这个工程痛点，感受 Pre-LN 的价值。

| 日期 | 任务 |
|---|---|
| D1 | 复现 Post-LN 训练到 5K~10K step，观察 loss 震荡/发散现象（作为对照组）|
| D2 | 实现 Pre-LN 变体：LayerNorm 移到子层输入之前 |
| D3 | 实现残差初始化缩放：`0.02 / sqrt(2*n_layers)` |
| D4 | 同数据对比 Post-LN vs Pre-LN 训练曲线；记录两者稳定性差异 |
| D5 | 加深加宽：层数 4→8，上下文 256→512；观察显存与训练时间变化 |
| D6 | 实现学习率 warmup + cosine 的精细调参；使用梯度裁剪 |
| D7 | **验收**：Pre-LN 版本训练到 10K step 无发散；loss 对比表新增一行 |

**本周产出**：训练稳定的 GPT-2 风格模型；理解 LN 位置对训练的影响。

---

### 第 7 周：生成、采样与评测（阶段 3 完成）

**主题**：让模型"像样地说话"，学会评测生成质量。

| 日期 | 任务 |
|---|---|
| D1 | 实现 temperature 采样：`logits / T` + softmax；观察 T=0.1/0.8/1.5 的差异 |
| D2 | 实现 top-k 与 top-p（nucleus）采样；对比二者行为 |
| D3 | 实现贪心解码与 beam search（可选），理解两者的取舍 |
| D4 | 构建生成评测脚本：多组 prompt、多组采样参数，自动输出对比样例 |
| D5 | 计算困惑度（perplexity）作为量化指标；在 train/valid 上分别统计 |
| D6 | 上下文扩展实验：把阶段 3 模型放到 1024 上下文，观察长文生成连贯性（可与阶段 1 对比）|
| D7 | **验收**：一个可交互的 CLI 生成器（输入 prompt 输出文本）；写一份"采样参数调参笔记" |

**本周产出**：交互式生成 demo + 评测工具链（ppl、采样参数控制）。

---

### 第 8 周：Llama 化改造（阶段 4 完成）

**主题**：把模型升级为现代架构，完成本计划最核心的一次重构。

| 日期 | 任务 |
|---|---|
| D1 | 实现 RMSNorm，与 LayerNorm 做数值对比；理解其省去均值的原因 |
| D2 | 实现 RoPE：旋转位置编码的公式推导 + `apply_rotary_pos_emb` 实现 |
| D3 | 实现 SwiGLU 门控 FFN（对比原 FFN 的维度与激活函数）|
| D4 | 实现 GQA：分组 KV 头，理解显存节省原理；实现 KV-cache（推理加速）|
| D5 | 整合四项改造为新的 `LlamaConfig` 架构；确认与阶段 3 参数量相当 |
| D6 | 同一数据训练阶段 3 与阶段 4，完成全量对比实验（loss/速度/显存/长文能力）|
| D7 | **验收**：阶段 4 对比报告完成；模型能外推生成较长文本；参看 MiniMind / Llama 官方实现对照检查 |

**本周产出**：Llama 风格 0.5B 模型 + 完整的四阶段架构演进对比实验表。

> 里程碑：你已经"亲手走完"2017→2023 的架构演进史。

---

### 第 9 周：正式中文数据集与清洗流水线

**主题**：把 toy 数据升级为生产级训练集，构建数据工程能力。

| 日期 | 任务 |
|---|---|
| D1 | 下载 SkyPile 子集（如 10~20GB）与中文维基 dump；验证 HuggingFace/ModelScope 下载链路 |
| D2 | 实现清洗流水线框架：`raw → clean → dedup → filter → shard` 的分步管线 |
| D3 | 实现去重（MinHash/SimHash 近似去重）+ 长度过滤 + 符号密度过滤 |
| D4 | 实现语言识别过滤（langid/fasttext）；构建混合配比逻辑（中英/百科/代码）|
| D5 | 用自研 tokenizer 对清洗后数据做全量编码；统计 token 量、重复率等质量指标 |
| D6 | 小批量抽样人工检查数据质量；修正清洗规则；迭代 2 轮 |
| D7 | **验收**：得到 5~15GB 干净中英混合语料 + 清洗报告（去重率、过滤比例、最终 token 数）|

**本周产出**：生产级训练集 + 可复用的数据清洗流水线代码。

---

### 第 10 周：训练优化与工程化

**主题**：解决"如何跑得更快、显存装得下"，补齐训练工程硬技能。

| 日期 | 任务 |
|---|---|
| D1 | 实现混合精度训练（BF16 `torch.autocast` + GradScaler）；对比吞吐提升 |
| D2 | 实现梯度累积（gradient accumulation），突破 batch size 显存限制 |
| D3 | 集成 WandB 或本地日志：loss/lr/grad-norm 实时可视化 |
| D4 | 实现 checkpoint 机制：断点续训、最佳模型保存、异常恢复 |
| D5 | 集成 Flash Attention（可选，flashattn 包）；对比前向速度 |
| D6 | 学习 FSDP/DeepSpeed 原理（了解即可）；可选：双卡 4090 跑通 DDP |
| D7 | **验收**：优化后的训练管线 smoke test 通过；输出一份"速度/显存优化前后对比表" |

**本周产出**：生产级训练管线（混合精度 + 梯度累积 + 日志 + checkpoint）。

---

### 第 11 周：正式全量训练（云 GPU）

**主题**：上云，用正式数据训练 0.5B 模型。

| 日期 | 任务 |
|---|---|
| D1 | 注册 AutoDL/腾讯云等平台；配置 4090 实例；打环境快照（装好全部依赖）|
| D2 | 上传代码与数据；跑 smoke test（1000 step，验证管线与数据无 bug）|
| D3 | 启动正式训练（2~5B token）；建立监控：loss 曲线、学习率曲线、显存 |
| D4 | 训练中期检查（约 20% 进度）：生成样例评估，必要时调整学习率/数据配比 |
| D5 | 继续训练；同时准备评估集（百科问答、日常对话、续写任务）|
| D6 | 训练完成：跑最终评估（困惑度 + 多任务生成样例），与阶段 4 小模型对比 |
| D7 | **验收**：模型训练完成；评估报告出炉；备份 checkpoint |

**本周产出**：正式训练完成的 0.5B 中文/双语模型 + 评估报告。

---

### 第 12 周：部署、收尾与总结

**主题**：让模型真正可用，输出完整的个人学习成果。

| 日期 | 任务 |
|---|---|
| D1 | 导出模型为 GGUF 格式（`llama.cpp`）；接入 Ollama，本地可用 |
| D2 | 可选：vLLM 部署（并发推理、OpenAI 兼容 API）|
| D3 | 编写《架构演进实验报告》：六阶段 loss 对比表、每处改进的关键发现、训练经验 |
| D4 | 可选：搭建 RAG 个人知识库（Dify/AnythingLLM），把模型接入你的知识积累 |
| D5 | 复盘：哪些架构改进收益最大？哪些坑值得记录？写进 README/博客 |
| D6 | 规划下一步：1.3B 模型？RLHF/DPO？量化部署？领域微调？ |
| D7 | **验收**：模型日常可用 + 完整实验报告 + 代码仓库整理归档（LICENSE/README/文档）|

**本周产出**：可日常使用的模型 + 架构演进实验报告 + 归档完整的学习仓库。

---

## 六、训练工程要点速查

### 超参数起步建议（0.5B）

| 超参数 | 建议值 | 说明 |
|---|---|---|
| Batch size | 256~512 sequences | 显存不够用梯度累积凑 |
| Seq len | 512（实验）/ 1024（正式）| 逐步加大 |
| Peak LR | 1e-3（配 256 batch）| 大 batch 配大 lr |
| Warmup steps | 500~2000 | 避免早期发散 |
| 学习率计划 | cosine decay 到 1e-4 | 或到达峰值×0.1 |
| Weight decay | 0.1（AdamW）| 只对权重，不 bias/LN |
| 梯度裁剪 | 1.0 | 稳定性保险 |
| 精度 | BF16 | RTX 4090 原生支持 |

### 常见坑清单

1. **位置编码重复**：实现 GPT-2 后忘了 RoPE 冲突 → 确保每个阶段只用一种位置编码
2. **mask 用错维度**：`(T,T)` 因果 mask 广播错误 → 写单元测试验证
3. **数据泄漏**：train/valid 切分前先打乱，按文档边界切，避免一句话被切进两个集合
4. **学习率过大直接 NaN**：先用 1e-4 起步验证，再调到 1e-3
5. **梯度累积忘记归一化**：loss 需除以累积步数
6. **实验对比不公平**：参数/数据/token 数必须一致，只允许改架构

### 实验记录模板（每阶段必填）

```
阶段：___    日期：___
数据：TinyStories / ___（token 数：___）
参数量：___   层数：___   头数：___   d_model：___
训练量：___ token   总耗时：___  吞吐：___ tok/s
最终 train loss：___    valid loss：___
架构改动点：___
观察到的差异：___
生成样例：___
```

---

## 七、风险与对策

| 风险 | 对策 |
|---|---|
| 中途放弃（最常见）| 每周验收节点强制产出可见成果；小步快跑，每阶段都有可演示 demo |
| 数学卡壳 | 3 条线并行：官方论文 + Karpathy 视频课 + MiniMind 源码注释 |
| 训练不出好效果 | 先用 TinyStories 把管线调通；不追求完美，0.5B 模型"能用"即达标 |
| 云成本失控 | 全部实验走按量计费；长任务前必跑 smoke test；打快照 |
| 下载数据集被墙/限速 | 用 ModelScope（国内）；HF 镜像 hf-mirror.com；或下载预清洗子集 |
| 显存不够 | 缩小 seq len/batch → 梯度累积 → 降到 0.3B → 换 40GB 卡 |

---

## 八、参考资源清单

### 必读/必看（按顺序）

1. Karpathy《Let's build GPT: from scratch》视频课（阶段 2~3 主参考）
2. Karpathy `nanoGPT` 仓库（阶段 2~3 代码参考）
3. Karpathy `minbpe` 仓库（阶段 0 tokenizer）
4. 《Attention Is All You Need》论文（阶段 1）
5. GPT-1 / GPT-2 论文（阶段 2~3）
6. Llama 1/2 论文 + `MiniMind` 中文项目（阶段 4）
7. Karpathy《Intro to Large Language Models》（1 小时，建立全局观，第 1 周看）

### 工具与平台

| 用途 | 推荐 |
|---|---|
| 训练框架 | 自研为主 + PyTorch |
| 微调/对比（可选）| LlamaFactory、Unsloth |
| 推理部署 | Ollama（本地）、vLLM（云端）、llama.cpp（GGUF 导出）|
| 云 GPU | AutoDL、恒源云（性价比）；腾讯云（稳定性）|
| 数据下载 | HuggingFace、ModelScope、hf-mirror.com |
| 实验追踪 | WandB / TensorBoard / 本地 JSON |

### 数据集直达

- TinyStories：ModelScope `AI-ModelScope/TinyStories` / HF `roneneldan/TinyStories`
- WikiText-103：HF `wikitext`（`wikitext-103-raw-v1`）
- SkyPile-150B：HF `Skywork/SkyPile-150B`
- MNBVC：GitHub `esbatmop/MNBVC`
- 中文维基 dump：dumps.wikimedia.org（zhwiki-latest-pages-articles.xml.bz2）
- SlimPajama：HF `cerebras/SlimPajama-627B`
- FineWeb-Edu：HF `HuggingFaceFW/fineweb-edu`

---

## 附：一键自检清单（每周日核对）

- [ ] 本周代码提交到 Git（含 commit message）
- [ ] 本周实验记录表已填写
- [ ] 本周验证点全部打勾
- [ ] 模型/脚本可被下周继续复用（README 更新）
- [ ] 下一周的数据/环境已准备就绪

---

*本计划基于"架构演进主线 + 一次只改一处"方法论设计。祝你玩得开心，把大模型彻底变成自己的东西。*
