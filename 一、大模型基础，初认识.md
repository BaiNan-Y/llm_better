## ## 创建 python 虚拟环境实现环境隔离

安装并创建python环境：

创建一个python环境;

```
python -m venv .llm_learn_venv
```

切换到这个环境上

```
llm_learn_venv\Scripts\Activate.ps1
```

## 安装Pytorch 2.x

因为我自己是N卡，我找了一个适合自己的CUDA版本：

```
python -m pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
```

安装CUDA之后，可以输入如下命令来进行验证：

```
python -c "import torch; print(torch.__version__)"
```

或者我们更习惯于写一个脚本来进行验证：

```
import torch

print(torch.cuda.is_available())
```

就可以看出来，我们的pytorch和cuda都安装好了

## 本地安装Ollama进行测试学习对比

或者这里其实可以租一个算力服务器来进行学习：

我这里在autodl 上租的5090

- Ollama安装

```
nvidia-smi 来确认显卡型号

## autodl 开启github加速
source /etc/network_turbo

## 针对于Linux系统，可以实现 一键安装 ollama
curl -fsSL https://ollama.com/install.sh | sh

## 记得把加速关了避免影响之后的下载问题
unset http_proxy https_proxy

## 确认ollama已经安装
ollama --version
```

- Ollama环境配置

我们先创建一个ollama的模型保存目录，这个可以选择自己喜欢的目录：

```
mkdir -p /root/autodl-tmp/ollama/models

# 在 /.bashrc 文件中将我们的ollama目录写入并令其生效，可以参考如下代码：
echo 'export OLLAMA_MODELS=/root/autodl-tmp/ollama/models' >> ~/.bashrc
source ~/.bashrc
```

- Ollama开启

```
# 开启服务并记录日志
nohup ollama serve > /root/autodl-tmp/ollama.log 2>&1 &

# 验证：
curl http://localhost:11434
# 返回 "Ollama is running" 即成功
```

- 下载 Qwen-1.5B模型

若网络不通畅，则需要另外的模型了

```
ollama pull qwen2.5:1.5b
# 查看已安装的模型
ollama list
# 启动已安装的模型
ollama run qwen2.5:1.5b
# 可以进行对话尝试

# 结束语
/Bye
```















































